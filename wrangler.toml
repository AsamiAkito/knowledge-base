# Cloudflare Workers 静态站点配置
# 把整个仓库根目录作为静态资源发布（本站是纯静态页面，无需构建）
# 注意：name 必须与 Cloudflare 上的 Worker 名一致，否则构建会部署不到位
name = "knowledge-base"
compatibility_date = "2026-08-01"
workers_dev = true

[assets]
directory = "./"
